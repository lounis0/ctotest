export const AboutPage = () => (
  <section className="card">
    <h2>About this starter</h2>
    <ul>
      <li>Shared ESLint + Prettier rules keep the stack consistent.</li>
      <li>Vitest powers unit tests on both the API and the UI.</li>
      <li>Docker Compose ships PostgreSQL plus dev servers with one command.</li>
    </ul>
    <p>Extend this page with project-specific copy once you are ready.</p>
  </section>
);
